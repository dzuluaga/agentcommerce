[project]
name = "agentcommerce"
version = "0.1.0"
description = "Multi-agent autonomous commerce system with verifiable digital credentials"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "websockets>=13.0",
    "openai>=1.50.0",
    "tavily-python>=0.5.0",
    "pydantic>=2.9.0",
    "python-dotenv>=1.0.0",
]
